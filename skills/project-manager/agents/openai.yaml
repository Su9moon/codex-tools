interface:
  display_name: "Project Manager"
  short_description: "Run lightweight, file-backed project workflows."
  default_prompt: "Use $project-manager to start and track this task with minimal context."
